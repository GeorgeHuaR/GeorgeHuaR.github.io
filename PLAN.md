

【v7.3.5】【右侧时间日期显示讨论】
- 直接限制当前的时间日期比较容易，但如何协调样式我没想好
- 能否获取当月日历？显示一个日历表（网络上应该有很多类似的Demo）
- 右侧整体空间比较大，还能添加什么修饰？
- 移动端适配是否有影响？我觉得移动端可以不现实右侧的Panel

先做时间 + 月历，无需月份切换，无需点击日期

当前的EditorPage,主卡片内容并不多，但是整个右侧main-content还是有滚动条，这是为何？

个人介绍页的滚动条样式没有同步，请检查（是否需要对当前主题单独设置？）


【补充说明】
我测试新增很多TODO项，超出了显示范围后，右侧多了一个默认样式的滚动条！原先我观察到的是当前主题风格的滚动条

内部滚动条样式可以先不改，我担心的是内容变多后出现两个滚动条影响操作
Fix 1 修复后：.main-content 不再溢出 → 外层滚动条消失 → 仅保留编辑器内部的 .editor-todo-list 自身滚动条，不会出现两个滚动条打架的情况。

【V7.3.6】滚动条修复
- 利用 .main-content 已有的 position: relative，对 .e区ditor-wrapper 用绝对定位精确填充内容
  .main-content 不再溢出 → 外层滚动条消失 → 仅保留编辑器内部的 .editor-todo-list 自身滚动条



